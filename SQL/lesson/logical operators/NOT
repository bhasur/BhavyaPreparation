The SQL Server (Transact-SQL) NOT Operator is used to display a record where the specified condition is not true.

Syntax:
The syntax for using NOT operator in SQL Server (Transact-SQL) is given below:

SELECT column1, column2, column3, ...
FROM table_name
WHERE NOT condition;

Example:
To select all records of the Employee table where City is NOT "New Delhi", the query is given below.

SELECT * FROM Employee
WHERE NOT City = 'New Delhi';
