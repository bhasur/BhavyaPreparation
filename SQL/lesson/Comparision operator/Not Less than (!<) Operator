The SQL Server (Transact-SQL) !< (not less than) operator checks if the value of left operand is not less than the value of right operand and returns true if the condition is true, false otherwise.

Syntax
The syntax for using not less than operator in SQL Server (Transact-SQL) is given below:

expression !< expression  

To select records of table where Age of the person is not less than 27, the query is given below.

SELECT * FROM Employee
WHERE Age !< 27;
